<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:fresco="http://schemas.android.com/tools">

    <com.facebook.drawee.view.SimpleDraweeView
        android:id="@+id/drawee_item"
        fresco:placeholderImage="@color/colorWhite"
        android:layout_width="@dimen/_100sdp"
        android:layout_height="@dimen/_100sdp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <ImageView
        android:id="@+id/imageView_Video"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:src="@drawable/vid_play"
        android:visibility="gone"
        app:layout_constraintBottom_toBottomOf="@+id/drawee_item"
        app:layout_constraintEnd_toEndOf="@+id/drawee_item"
        app:layout_constraintStart_toStartOf="@+id/drawee_item"
        app:layout_constraintTop_toTopOf="@+id/drawee_item" />

    <ProgressBar
        android:layout_centerInParent="true"
        android:id="@+id/pbLoading"
        android:layout_width="30dp"
        android:layout_height="30dp"
        android:textAlignment="center"
        app:layout_constraintBottom_toBottomOf="@+id/drawee_item"
        app:layout_constraintEnd_toEndOf="@+id/drawee_item"
        app:layout_constraintStart_toStartOf="@+id/drawee_item"
        app:layout_constraintTop_toTopOf="@+id/drawee_item"/>


</androidx.constraintlayout.widget.ConstraintLayout>